---
title: Included upstream changes
description: The upstream vLLM and B12X pull requests carried by the current locked build.
---

Published images are built from pinned fork revisions of [`local-inference-lab/vLLM`](https://github.com/local-inference-lab/vllm) and [`local-inference-lab/B12X`](https://github.com/local-inference-lab/b12x), not from their upstream branches. A revision alone does not say which proposed changes it carries, so this page lists them.

A change reaches the build in one of two ways. It is either already merged into the pinned fork revision, or it is applied to the pinned source at build time from a patch in this repository. Both are recorded below, and the same list is written into the image's `org.opencontainers.image.description` label.

{/* Generated by scripts/vllmb12x-included-changes.py. Do not edit by hand. */}

{/* included-changes:start */}
## vLLM

Pinned at [`bd22e0f25043`](https://github.com/randomvariable/vllm/commit/bd22e0f250439659d9fe30903b7aa124c321a932), branched from [`local-inference-lab/vllm`](https://github.com/local-inference-lab/vllm) `dev/jovian-judgement`.

- **[local-inference-lab/vllm#777](https://github.com/local-inference-lab/vllm/pull/777)** — fix(qwen): propagate MTP positional overrides
  - Merged into the pinned revision (3 commits).
  - The draft model resolved 262,144 positions from its checkpoint while the target resolved 1,048,576, so a full-length prefill was rejected.
- **[local-inference-lab/vllm#779](https://github.com/local-inference-lab/vllm/pull/779)** — perf(qwen): shard TP4 HC prefill and coalesce recurrent checkpoints
  - Merged into the pinned revision (9 commits).
  - Opt-in: the tail coalescing and token-ownership paths are off unless the deployment enables them.
- **[vllm-project/vllm#52917](https://github.com/vllm-project/vllm/pull/52917)** — Adaptive spin grace and bounded architectural waits for shm_broadcast
  - Applied at build time by `third_party/vllm_shm_broadcast_spin_grace.patch`.
  - The pinned revision carries the bounded-wait half. The patch adds the adaptive grace half, reproducing randomvariable/vllm refs/heads/dev/rv-jovian-judgement at 3c948029c899219ce5d7c4bbb565e20d7bc6a837. Keep it for every tensor-parallel model: without it the mp executor busy-spins.
- **Rewrite flash_attn.cute imports to vllm.vllm_flash_attn.cute** — no upstream pull request
  - Applied at build time by `third_party/vllm_flash_attn_cute_namespace.patch`.
  - Packaging repair for this builder's symlink install path, which skips the import rewrite the upstream CMake copy performs. It carries no upstream change.

## B12X

Pinned at [`4401ce4fb982`](https://github.com/randomvariable/b12x/commit/4401ce4fb98265c3b1e1579ae03960ff41d84d50), branched from [`local-inference-lab/b12x`](https://github.com/local-inference-lab/b12x) `master`.

- **[local-inference-lab/b12x#384](https://github.com/local-inference-lab/b12x/pull/384)** — fix(preparation): retain prepared launchers and coordinate collectives
  - Merged into the pinned revision (10 commits).
  - Includes the RoCE one-shot dtype fix, the collective entry barrier, and the source-hashed JIT extension build directory.
- **[local-inference-lab/b12x#386](https://github.com/local-inference-lab/b12x/pull/386)** — feat(ple): export prepared internal prefill checkpoints
  - Merged into the pinned revision (2 commits).
- **[local-inference-lab/b12x#387](https://github.com/local-inference-lab/b12x/pull/387)** — perf(qsa): reuse representative keys across paired queries
  - Merged into the pinned revision (4 commits).
{/* included-changes:end */}

## Keeping this list honest

`scripts/vllmb12x-included-changes.py --check` fails when this page, the README table, or the image description no longer matches the ledger, and when the recorded patch series no longer matches the patches Bazel applies.

`scripts/vllmb12x-included-changes.py --verify --clone-root <directory>` goes further and checks the ledger against the fork history: a pull request is integrated by replaying its commits, so the command asserts that every commit of every listed pull request is present in the pinned revision, and that the recorded commit count still matches.
