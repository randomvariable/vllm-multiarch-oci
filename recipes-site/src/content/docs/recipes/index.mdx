---
title: Recipe catalog
description: Digest-pinned vLLM deployments with explicit validation status.
---

import RecipeCatalog from "../../../components/RecipeCatalog.astro";

<p class="eyebrow">Recipe catalog</p>

# Deployments with receipts

A recipe fixes the engine behavior that was accepted. Host paths, network devices, allocatable resource names, selectors, and credentials remain deployment inputs. The validated image is always the default; a newer publication is offered separately and never inherits the recipe's acceptance status.

<RecipeCatalog />
