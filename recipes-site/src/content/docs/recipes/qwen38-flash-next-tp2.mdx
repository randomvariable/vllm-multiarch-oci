---
title: Qwen3.8 Flash Next on two DGX Spark systems
description: The tested two-node Qwen3.8-Flash-Next-NVFP4 deployment recipe.
sidebar:
  label: Qwen3.8 Flash Next
---

import RecipeBuilder from "../../../components/RecipeBuilder.astro";

<p class="eyebrow">Runtime recipe · Qwen3.8 Flash Next</p>

This is the public, parameterized form of the two-node NVIDIA DGX Spark deployment I run for Qwen3.8-Flash-Next-NVFP4. Its image contains the pinned [`local-inference-lab/vLLM`](https://github.com/local-inference-lab/vllm) fork rather than an arbitrary upstream vLLM release. The recipe fixes the model revision, engine flags, environment, topology, and validated image. You supply the details that belong to your cluster or Docker hosts.

<div class="release-strip" aria-label="Recipe validation status">
  <span>Validated image</span>
  <code>ghcr.io/randomvariable/vllm-b12x-multi@sha256:3db7f3d50d139a4e07acbda9e4caf8815a740d31981224d707466d150c331956</code>
  <a href="#recipe-builder-title">Configure</a>
</div>

<dl class="fact-grid">
  <div><dt>Model</dt><dd>local-inference-lab/Qwen3.8-Flash-Next-NVFP4</dd></div>
  <div><dt>Model revision</dt><dd>629bc3218833a38b475b719f34aa571666f4a03e</dd></div>
  <div><dt>Topology</dt><dd>2 nodes · TP=2 · one DGX Spark per node</dd></div>
  <div><dt>Context</dt><dd>1,048,576 tokens</dd></div>
</dl>

## What is fixed

The recipe preserves the live runtime command and tuning accepted on 2026-09-18: B12X GDN prefill and decode, linear and MoE backends, buffered InstantTensor loading, NVFP4 weights through `modelopt_mixed`, MTP speculation at depth 3, FP8 KV cache, full and piecewise CUDA graphs, GPU utilization `0.77`, eight sequences, and an 8,192-token batch budget. It also selects the tested A16 dense-activation path, one-shot RoCE all-reduce limits, adaptive shared-memory broadcast grace, and the image's AOT artifacts.

Two settings deserve attention because they are not interchangeable with defaults.

The PLE tables live on disk. `--additional-config '{"ple_table_memory":"disk"}'` sends the roughly 26.8 GiB of n-gram tables to the B12X io_uring `DiskTable`, which reads rows straight out of the checkpoint's safetensors by byte offset. Those tables never occupy VRAM or host DRAM, which is what makes the model fit on a unified-memory node. The consequence is that the published model directory must stay readable on local storage for the entire life of the process, not just during load, and the loader must be `instanttensor`.

The context comes from a YaRN override. The checkpoint declares `max_position_embeddings` 262,144 with `rope_type` `default`, so the 1,048,576-token window is a 4x override applied through `--hf-overrides`. The override keeps `mrope_section` and `mrope_interleaved` because `get_rope()` only preserves M-RoPE, which the vision position ids need, when `mrope_section` is present. Serving this at 1M tokens also requires vLLM `5a70cefff7e0` or later: earlier revisions did not pass the override into the MTP draft model, so the draft stayed at 262,144 and rejected a full-length prefill.

This is the LWS configuration I run. The Docker target uses the same engine definition, but I have not run it on two separate hosts.

The [deployment benchmark guide](/vllm-multiarch-oci/guides/benchmark-deployment/) explains how to run the benchmark against another deployment.

## Measured performance

<div class="benchmark-panel">
  <div class="benchmark-context">
    <span>Decode capacity sweep</span>
    <span>Benchmark v0.4.29</span>
    <span>30 seconds per cell</span>
    <span>Concurrency 1, 2, 4, 8</span>
    <span>Contexts 0 to 128K</span>
  </div>
  <div class="benchmark-table" role="table" aria-label="Qwen3.8 Flash Next deployment benchmark results">
    <div class="benchmark-row benchmark-header" role="row">
      <span role="columnheader">Prompt context</span>
      <span role="columnheader">C=8 aggregate decode</span>
      <span role="columnheader">C=8 per-user decode</span>
      <span role="columnheader">Integrated prefill</span>
      <span role="columnheader">Scheduler state</span>
    </div>
    <div class="benchmark-row" role="row">
      <strong role="cell">0</strong>
      <span role="cell" class="benchmark-primary"><b>182.1</b> tok/s</span>
      <span role="cell"><b>22.8</b> tok/s</span>
      <span role="cell">Not measured</span>
      <span role="cell">8 / 8 running</span>
    </div>
    <div class="benchmark-row" role="row">
      <strong role="cell">8K</strong>
      <span role="cell" class="benchmark-primary"><b>178.7</b> tok/s</span>
      <span role="cell"><b>22.3</b> tok/s</span>
      <span role="cell"><b>2,524</b> tok/s</span>
      <span role="cell">8 / 8 running</span>
    </div>
    <div class="benchmark-row" role="row">
      <strong role="cell">32K</strong>
      <span role="cell" class="benchmark-primary"><b>175.7</b> tok/s</span>
      <span role="cell"><b>22.0</b> tok/s</span>
      <span role="cell"><b>2,401</b> tok/s</span>
      <span role="cell">8 / 8 running</span>
    </div>
    <div class="benchmark-row" role="row">
      <strong role="cell">64K</strong>
      <span role="cell" class="benchmark-primary"><b>171.0</b> tok/s</span>
      <span role="cell"><b>21.4</b> tok/s</span>
      <span role="cell"><b>2,311</b> tok/s</span>
      <span role="cell">8 / 8 running</span>
    </div>
    <div class="benchmark-row" role="row">
      <strong role="cell">128K</strong>
      <span role="cell" class="benchmark-primary"><b>181.0</b> tok/s</span>
      <span role="cell"><b>22.6</b> tok/s</span>
      <span role="cell"><b>2,147</b> tok/s</span>
      <span role="cell">8 / 8 running</span>
    </div>
  </div>
  <p class="benchmark-note"><strong>Method:</strong> this unprofiled sustained-decode run used a direct port-forward from the benchmark client, max output length 8,192, integrated prefill scouts, and one 30-second cell for each concurrency and context combination. vLLM metrics were available. No hardware panel was collected because the client did not have <code>nvidia-smi</code>.</p>
  <p class="benchmark-note"><strong>Capacity:</strong> each C=8 cell reached eight running requests with no queue. The recipe sets <code>--max-num-seqs 8</code>, so C=8 fills the scheduler's sequence slots. The reported aggregate is duration-weighted per-request decode, not total server throughput, because concurrent generation intervals overlap.</p>
  <p class="benchmark-note"><strong>Provenance:</strong> the deployment used GitOps revision <code>0527ce13c</code>, the live LWS reached 2/2 ready, and the runtime selected <code>VLLM_B12X_DENSE_ACTIVATION_MODE=a16</code>. Do not compare this result directly with the older max-seqs=4 baseline because that baseline used a different dense-activation configuration.</p>
</div>

:::caution[Allocator units are not physical devices]
Kubernetes extended-resource counts are values published by the cluster's device plugins. A site may advertise more allocator units than physical hardware. The beginner path requests one GPU and one RDMA unit per rank; experienced operators must match the generated requests to their own allocator policy.
:::

Requests enter at rank zero and collectives cross the secondary network; the shape is the same for every recipe here and is described in <a href={`${import.meta.env.BASE_URL}explanation/tp2-traffic-paths/`}>traffic paths in a two-node TP=2 group</a>.

<RecipeBuilder slug="qwen38-flash-next-tp2" />

## Before applying output

- Each GPU node needs at least 130 GiB free on the model path. The checkpoint's tensor payload is about 99 GiB and it stays on disk for the life of the process.
- For Kubernetes, complete the <a href={`${import.meta.env.BASE_URL}guides/cluster-prerequisites/`}>cluster prerequisite checklist</a> and confirm the NVIDIA runtime, LWS controller, CNI, Multus, and RDMA allocator are ready.
- For Docker, synchronize the model on both hosts before starting either serving container. Start rank zero first, then let the worker's rendezvous check succeed before its headless server starts.
- Stop a GB10 serving container gracefully with a 120-second timeout. Long kernel execution is not grounds for force-killing a GPU process.
- A digest-qualified latest image is newer, not equivalent to the validated recipe image. Selecting it changes the validation status shown by the generator.
