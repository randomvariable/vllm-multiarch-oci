---
title: Guides
description: Host, cluster, deployment, and routing guides for the recipe catalog.
---

# Guides

Use the <a href={`${import.meta.env.BASE_URL}tutorials/k3s-to-deepseek/`}>beginner walkthrough</a> for a linear route from two prepared GB10 hosts to routed inference. Operators bringing an existing cluster can enter through the <a href={`${import.meta.env.BASE_URL}guides/cluster-prerequisites/`}>cluster prerequisite checklist</a> and then open the TP=2 recipe.
