# Source comments and docstrings remain authoritative. These entries fill the
# gaps where the integration exposes a control without user-facing prose.
descriptions:
  B12X_AUTOTUNE: "Enable B12X kernel autotuning during preparation."
  B12X_AUTOTUNE_EXHAUSTIVE: "Extend B12X autotuning to the exhaustive search space."
  B12X_COLLECTIVE_BARRIER_TIMEOUT: "Maximum preparation time in seconds for cross-rank collective readiness."
  B12X_COMPILE_*: "Enable or size B12X generated-kernel compilation caches."
  B12X_DENSE_*: "Select a B12X dense GEMM specialization or its workspace behavior."
  B12X_DIRECT_CUTE_OPTIONS: "Pass direct CuteDSL compiler options to the B12X MoE path."
  B12X_DISABLE_BF16_GEMV: "Disable the B12X BF16 GEMV fast path when set."
  B12X_DISK_BACKEND: "Select the disk-backed PLE row-staging backend: io_uring or GDS."
  B12X_DSA_VALIDATE_PAGE_IDS: "Validate DSA page identifiers in the B12X indexer path."
  B12X_DYNAMIC_SWAP_AB: "Select the dynamic A/B swap policy for B12X fused MoE kernels."
  B12X_DYNAMIC_TILE_MN: "Select the dynamic M/N tile policy for B12X fused MoE kernels."
  B12X_ENABLE_FP6_MICRO: "Enable the B12X FP6 micro-kernel path."
  B12X_ENGINE_STARTED: "Mark that the vLLM engine has started for post-start diagnostics."
  B12X_FP6_ACT_FMT_OVERRIDES: "Override FP6 activation formats selected by the B12X checkpoint."
  B12X_FUSED_INDEXER: "Enable the fused B12X sparse indexer path."
  B12X_HANG_DUMP: "Write B12X preparation state when a suspected hang is detected."
  B12X_LOG_CUTE_COMPILES: "Enable B12X CuteDSL compilation progress logging."
  B12X_LOG_CUTE_COMPILES_AFTER_ENGINE_START: "Enable B12X CuteDSL compile logging after engine startup."
  B12X_LOG_CUTE_COMPILE_ARGS: "Log arguments passed to B12X CuteDSL compilation."
  B12X_LOG_CUTE_COMPILE_STACK: "Log the Python stack for B12X CuteDSL compilation."
  B12X_LOG_CUTE_COMPILE_STACK_DEPTH: "Set the maximum stack depth in B12X CuteDSL compile logs."
  B12X_MHC_PDL: "Enable programmatic dependent launch for MHC normalization kernels."
  B12X_MHC_PREFILL_THREADS: "Set the CUDA thread count for the MHC prefill kernel."
  B12X_MHC_PREFILL_GRAM_THREADS: "Set the CUDA thread count for the MHC Gram-trick prefill path."
  B12X_MHC_PREFILL_FINALIZE_THREADS: "Set the CUDA thread count for the MHC prefill finalize kernel."
  B12X_MHC_PREFILL_TMA_WARPS: "Set the compute warp count for the MHC prefill TMA kernel."
  B12X_MHC_PREFILL_TMA_TILE_M: "Set the M tile dimension for the MHC prefill TMA kernel."
  B12X_MHC_PREFILL_TMA_TILE_N: "Set the N tile dimension for the MHC prefill TMA kernel."
  B12X_MHC_PREFILL_TMA_TILE_K: "Set the K tile dimension for the MHC prefill TMA kernel."
  B12X_MHC_PREFILL_TMA_STAGES: "Set the asynchronous pipeline stage count for the MHC prefill TMA kernel."
  B12X_MHC_PREFILL_TF32_TMA_M_WARPS: "Set the M-dimension compute warp count for the MHC TF32 TMA prefill kernel."
  B12X_MHC_PREFILL_TF32_TMA_N_WARPS: "Set the N-dimension compute warp count for the MHC TF32 TMA prefill kernel."
  B12X_MHC_PREFILL_TF32_TMA_WARPS: "Set the compute warp count for the MHC TF32 TMA prefill kernel."
  B12X_MHC_PREFILL_TF32_TMA_TILE_M: "Set the M tile dimension for the MHC TF32 TMA prefill kernel."
  B12X_MHC_PREFILL_TF32_TMA_TILE_N: "Set the N tile dimension for the MHC TF32 TMA prefill kernel."
  B12X_MHC_PREFILL_TF32_TMA_TILE_K: "Set the K tile dimension for the MHC TF32 TMA prefill kernel."
  B12X_MHC_PREFILL_TF32_TMA_STAGES: "Set the asynchronous pipeline stage count for the MHC TF32 TMA prefill kernel."
  B12X_MHC_PREFILL_TF32_TMA_CHUNK_MIN_TOKENS: "Set the token threshold that selects the MHC TF32 TMA chunk specialization."
  B12X_MHC_PREFILL_TF32_TMA_CHUNK_M_WARPS: "Set the M-dimension compute warp count for the MHC TF32 TMA chunk specialization."
  B12X_MHC_PREFILL_TF32_TMA_CHUNK_N_WARPS: "Set the N-dimension compute warp count for the MHC TF32 TMA chunk specialization."
  B12X_MHC_PREFILL_TF32_TMA_CHUNK_TILE_M: "Set the M tile dimension for the MHC TF32 TMA chunk specialization."
  B12X_MHC_PREFILL_TF32_TMA_CHUNK_TILE_N: "Set the N tile dimension for the MHC TF32 TMA chunk specialization."
  B12X_MHC_PREFILL_TF32_TMA_CHUNK_TILE_K: "Set the K tile dimension for the MHC TF32 TMA chunk specialization."
  B12X_MHC_PREFILL_TF32_TMA_CHUNK_STAGES: "Set the asynchronous pipeline stage count for the MHC TF32 TMA chunk specialization."
  B12X_MHC_PREFILL_TF32_TMA_LONG_MIN_TOKENS: "Set the token threshold that selects the MHC TF32 TMA long-context specialization."
  B12X_MHC_PREFILL_TF32_TMA_LONG_M_WARPS: "Set the M-dimension compute warp count for the MHC TF32 TMA long-context specialization."
  B12X_MHC_PREFILL_TF32_TMA_LONG_N_WARPS: "Set the N-dimension compute warp count for the MHC TF32 TMA long-context specialization."
  B12X_MHC_PREFILL_TF32_TMA_LONG_TILE_M: "Set the M tile dimension for the MHC TF32 TMA long-context specialization."
  B12X_MHC_PREFILL_TF32_TMA_LONG_TILE_N: "Set the N tile dimension for the MHC TF32 TMA long-context specialization."
  B12X_MHC_PREFILL_TF32_TMA_LONG_TILE_K: "Set the K tile dimension for the MHC TF32 TMA long-context specialization."
  B12X_MHC_PREFILL_TF32_TMA_LONG_STAGES: "Set the asynchronous pipeline stage count for the MHC TF32 TMA long-context specialization."
  B12X_MICRO_DYNAMIC_CUTOVER_PAIRS: "Set the active expert-pair count at which micro-MoE changes to its dynamic execution path."
  B12X_MICRO_SHARE_INPUT_ACROSS_EXPERTS: "Enable reuse of one micro-MoE input load across the selected experts."
  B12X_MLA_SM120_PREFILL_MG: "Enable the SM120 multi-group MLA prefill kernel."
  B12X_MLA_SM120_PREFILL_PACK_HILO_ROWS: "Pack high- and low-position MLA prefill rows into one SM120 kernel input."
  B12X_MSA_DECODE_PAGEMAX: "Enable the page-maximum reduction used by the MSA decode indexer."
  B12X_NVFP4_SPLIT_DECODE: "Split NVFP4 decode work into the materialized decode path instead of the monolithic path."
  B12X_PAGED_EXTEND_QWEN_FP8_PV_REPACK: "Enable Qwen FP8 value-projection repacking during paged attention extension."
  B12X_PAGED_INDEX_SUPERTILE_K: "Set the K span of each paged sparse-index prefill supertile."
  B12X_PAGED_KV_TMA_PLANE_SWIZZLE: "Set the TMA plane swizzle used to load paged KV data."
  B12X_PAGED_MSA: "Enable MSA block-sparse paged attention."
  B12X_PAGED_MSA_UNION_PREFILL: "Enable the MSA union-prefill path for paged attention."
  B12X_PREPARATION_TRACE_DIR: "Write B12X preparation timing traces to this directory."
  B12X_PROBE_*: "Configure B12X PCIe overlap probe diagnostics."
  B12X_SQG_XOR_CHEB_T12_SMEM: "Enable shared-memory storage for the SQG XOR-Chebyshev T12 kernel path."
  B12X_TIMING: "Enable B12X kernel timing output."
  B12X_TIMING_THRESHOLD_MS: "Set the B12X kernel timing output threshold in milliseconds."
  B12X_VALIDATE_*: "Enable B12X validation checks for runtime values."
  B12X_MOE_TILE_MN: "Override the MxN MMA tile shape used by the B12X fused MoE kernel."
  B12X_PCIE_ALLREDUCE_ALGORITHM: "Select the B12X PCIe all-reduce algorithm: automatic, hierarchical, island reduce-scatter, or one-shot."
  B12X_PCIE_DMA_A2A_CHUNKS: "Override the number of chunks used by the PCIe DMA all-to-all path."
  B12X_PCIE_DMA_FP8: "Enable the FP8 PCIe DMA all-reduce path."
  B12X_PCIE_DMA_PIECES: "Override the number of pieces used by the PCIe DMA all-reduce path."
  B12X_PCIE_FUSED_THREADS: "Set the CUDA thread count for the fused PCIe collective kernel."
  B12X_PCIE_HIERARCHICAL_BF16X2: "Enable vectorized BF16x2 loads in the hierarchical PCIe all-reduce kernel."
  B12X_PCIE_HIERARCHICAL_BF16X2_MAX_ELEMENTS: "Set the largest reduction size eligible for vectorized BF16x2 hierarchical PCIe all-reduce."
  B12X_PCIE_HIERARCHICAL_DEFERRED_CONSUMPTION: "Defer consumption of hierarchical PCIe all-reduce partial results."
  B12X_PCIE_HIERARCHICAL_DOUBLE_BUFFER: "Enable double buffering of staging memory in hierarchical PCIe all-reduce."
  B12X_PCIE_HIERARCHICAL_NANOSLEEP_CYCLES: "Set spin-wait nanosleep cycles for hierarchical PCIe all-reduce."
  B12X_PCIE_HIERARCHICAL_THREADS: "Set the CUDA thread count for hierarchical PCIe all-reduce."
  B12X_PCIE_ISLAND_RS_NANOSLEEP_CYCLES: "Set spin-wait nanosleep cycles for island reduce-scatter PCIe all-reduce."
  B12X_PCIE_ONESHOT_BLOCK_LIMIT: "Set the maximum number of CUDA blocks used by one-shot PCIe all-reduce."
  B12X_PCIE_ONESHOT_PUSH: "Enable the one-shot PCIe push transport, which writes each rank's input into every peer's eager slot."
  B12X_PCIE_ONESHOT_THREADS: "Set the CUDA thread count for one-shot PCIe all-reduce."
  B12X_PCIE_TEST_VISIBLE_SM_COUNT: "Override the visible SM count for PCIe collective topology validation."
  B12X_PCIE_TP2_PLAIN_REMOTE_PUSH: "Enable the qualified plain TP2 remote-write PCIe transport."
  B12X_PCIE_TP2_REMOTE_PUSH: "Enable the qualified fused TP2 remote-write PCIe transport."
  B12X_PCIE_TP4_REMOTE_PUSH: "Enable the qualified fused TP4 remote-write PCIe transport."
  B12X_PCIE_TP8_OWNER_REDUCE: "Enable the topology-scoped fused TP8 owner-reduce PCIe transport."
  B12X_PCIE_VOCAB_ARGMAX_NANOSLEEP_CYCLES: "Set spin-wait nanosleep cycles for PCIe vocabulary argmax reduction."
  B12X_ROCE_CACHE_DIR: "Directory for the compiled B12X RoCE proxy and its source-hash cache."
  B12X_ROCE_SPIN_LIMIT: "Bound the B12X RoCE preparation wait before the backend gives up."
  B12X_TUNING_CACHE_VERSION: "Version the B12X preparation tuning-cache format."
  B12X_W4A16_SMALL_M_DIRECT: "Use the direct small-M W4A16 MoE kernel instead of its fallback path."
  B12X_W4A16_SMALL_M_HOST_BARRIER_RESET: "Reset the small-M W4A16 MoE host barrier between launches."
  B12X_W4A16_SMALL_M_SPLITK: "Enable split-K decomposition for the small-M W4A16 MoE kernel."
  B12X_W4A8_*: "Configure the B12X W4A8 mixture-of-experts kernel path."
  VLLM_B12X_*: "Configure a local-inference-lab vLLM B12X execution path."
  VLLM_DEBUG_GRAPH_MEMORY_ACCOUNTING: "Log CUDA graph memory-accounting details."
  VLLM_ENABLE_PCIE_ALLREDUCE: "Enable the B12X PCIe all-reduce transport."
  VLLM_ENABLE_ROCE_ALLREDUCE: "Enable the B12X one-shot RoCE all-reduce transport."
  VLLM_GLM53_DFLASH_ATTN_LIB: "Set the shared library used by GLM5Next dflash attention instead of building it at runtime."
  VLLM_GLM53_DFLASH_ATTN_MAX_REQS: "Set the maximum request count admitted to GLM5Next dflash attention."
  VLLM_GLM53_DFLASH_ATTN_WS_REQS: "Set the dflash attention workspace capacity in requests for GLM5Next."
  VLLM_GLM53_KDA_GATE_SIDE_STREAM: "Run the GLM5Next KDA gate computation on a side stream; zero restores sequential forward execution."
  VLLM_GLM53_L2_PREFETCH: "Enable the GLM5Next SM120 L2-prefetch path."
  VLLM_GLM53_L2_PREFETCH_PERSIST_MB: "Reserve this many megabytes of persistent L2 cache per rank for GLM5Next prefetching."
  VLLM_GLM53_SPLIT_MAMBA_BLOCK_SIZE: "Set the Mamba cache block size when GLM5Next uses split KV cache geometry."
  VLLM_GLM53_SPLIT_TARGET_BLOCK_SIZE: "Set the target-model KV cache block size when GLM5Next uses split KV cache geometry."
  VLLM_MTP_NVFP4_LM_HEAD: "Enable the NVFP4 MTP language-model head path."
  VLLM_PCIE_ALLREDUCE_BACKEND: "Select the PCIe all-reduce backend; B12X enables the B12X PCIe communicator."
  VLLM_PCIE_ONESHOT_ALLREDUCE_MAX_SIZE: "Set the largest all-reduce payload routed to B12X PCIe one-shot transport."
  VLLM_PCIE_ONESHOT_FUSED_ADD_RMS_NORM_MAX_SIZE: "Set the largest fused add-and-RMS-norm payload routed to B12X PCIe one-shot transport."
  VLLM_PCIE_TWOSHOT_ROW_ELEMS: "Set the BF16 two-shot PCIe all-reduce row width in elements."
  VLLM_PLE_*: "Select the B12X PLE table placement or execution path."
  VLLM_QWEN3_8_*: "Select a Qwen3.8 Flash Next execution path."
  VLLM_ROCE_ALLREDUCE_MAX_SIZE: "Set the largest all-reduce payload routed to the B12X RoCE transport."
  VLLM_SHM_BROADCAST_*: "Tune adaptive shared-memory broadcast wait behavior."
